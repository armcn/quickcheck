#' @param frac_nan The approximate fraction of `NaN` values
#' to be included in the vector. A fraction of 0, the
#' default, will include no `NaN` values. A fraction of 0.5
#' would include around 50% `NaN` values.
