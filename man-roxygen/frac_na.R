#' @param frac_na The approximate fraction of `NA` values to
#' be included in the vector. A fraction of 0, the default,
#' will include no `NA` values. A fraction of 0.5 would
#' include around 50% `NA` values.
