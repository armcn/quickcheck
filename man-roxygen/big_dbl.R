#' @param big_dbl Should doubles near the maximum size be
#' included? This may cause problems because if the result
#' of a computation results in a double larger than the
#' maximum it will return `Inf`.
