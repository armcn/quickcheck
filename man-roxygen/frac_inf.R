#' @param frac_inf The approximate fraction of `Inf` and
#' `-Inf` values to be included in the vector. A fraction of
#' 0, the default, will include no `NA` values. A fraction
#' of 0.5 would include around 50% `NA` values.
